package abstraction_interface.assigment_problems;

// Interface defining offensive combat capabilities with an overloaded attack method.
public interface Attackable {
    String attack();
    String attack(String weaponName);
}
