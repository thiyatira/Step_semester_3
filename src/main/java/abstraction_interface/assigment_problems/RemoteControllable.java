package abstraction_interface.assigment_problems;

// Interface defining capability for devices and apps that connect to control hubs.
public interface RemoteControllable {
    String connect(String appId);
}
